[project]
name = "uwo-course-agent"
version = "0.1.0"
description = "An AI agent that helps students research and plan courses at Western University."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.2",
    "python-dotenv",
    "beautifulsoup4",
]
